# Canonical project metadata lives in pyproject.toml. Install with:
#
#     pip install -e .
#
# This file lists only the runtime dependencies, kept in sync with the
# [project.dependencies] table in pyproject.toml so legacy tooling that
# expects requirements.txt still works.
numpy>=1.20.0
scipy>=1.7.0
sympy>=1.8
matplotlib>=3.4.0
tqdm>=4.60.0
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
