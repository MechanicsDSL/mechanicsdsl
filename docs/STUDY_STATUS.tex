%% ============================================================================
%%  Silent-Failure Study --- One-Page Status
%%  Compile with:  pdflatex STUDY_STATUS.tex
%% ============================================================================

\documentclass[10pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\let\Bbbk\relax
\usepackage{newtxtext,newtxmath}
\usepackage[margin=0.6in,top=0.55in,bottom=0.5in]{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage[expansion=false]{microtype}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{titlesec}

\definecolor{okgreen}{RGB}{30,110,60}
\definecolor{nextblue}{RGB}{20,60,110}
\definecolor{holdred}{RGB}{150,45,45}
\definecolor{rulegrey}{RGB}{190,190,185}

\titleformat{\section}{\normalfont\large\bfseries}{}{0em}{}
\titlespacing*{\section}{0pt}{1.1ex plus .2ex}{0.45ex}

\renewcommand{\arraystretch}{1.05}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\newcommand{\done}{\textcolor{okgreen}{\textbf{\checkmark}}}
\newcommand{\todo}{\textcolor{nextblue}{$\square$}}

\pagestyle{empty}
\setlength{\parindent}{0pt}

\begin{document}
\small

{\LARGE\bfseries Silent-Failure Study}\\[0.3em]
{\large Status as of 22 August 2026}\\[0.2em]
{\color{rulegrey}\hrule height 1pt}

\vspace{0.9em}

{\large\bfseries Where you are:} \textbf{the baseline is locked.} Week 1 begins.\\[0.3em]
The measurement of your own engine is finished and reproducible. Everything
remaining is about the other two engines.

\vspace{0.5em}

% ---------------------------------------------------------------------------
\section{Done}

\begin{tabular}{@{}cL{0.88\textwidth}@{}}
\done & \textbf{Engine frozen} at \texttt{a8dc2b2}, tag \texttt{v2.1.3} (\texttt{FREEZE.md}). \\
\done & \textbf{Baseline measured and re-verified.} 55 cases: 44 pass, \textbf{0 silent}, 1 error, 10 timeout; 45 adjudicated. \\
\done & \textbf{Reproduction confirmed} --- bit-identical to the 10 August run. Zero status changes, no metric changed at all. This also forecloses ``could that be harness noise?'' against the week-2 comparison. \\
\done & \textbf{Third engine chosen:} Drake. \quad \done~\textbf{Documented:} freeze record (16\,pp) + fixes writeup (5\,pp). \\
\end{tabular}

% ---------------------------------------------------------------------------
\section{Next --- Week 1: adapters}

\textit{This is the hard part and where all the risk lives. Budget the whole week.}

\vspace{0.3em}
\begin{tabular}{@{}cL{0.88\textwidth}@{}}
\todo & \textbf{1. Library-independent $N$-link reference} (\texttt{numpy} only). Closed-form $M(q)\ddot q + C(q,\dot q)\dot q + g(q) = 0$. \textbf{Do this first} --- it is the ground truth the SymPy column needs, since a SymPy oracle checking a SymPy engine is near self-consistency. Doubles as the gate case. \\
\todo & \textbf{2. SymPy adapter.} Pendulum chain via \texttt{sympy.physics.mechanics}. Already installed, so it is the smaller of the two. \\
\todo & \textbf{3. Install Drake in Ubuntu WSL.} Not native Windows. The heavier install --- start it early, in parallel with steps 1--2. \\
\todo & \textbf{4. Drake adapter.} Same chain, same knobs. \\
\todo & \textbf{5. The gate:} all three engines agree with the reference on a hand-checked $N=2$ case. Do not proceed past this until they do. \\
\end{tabular}

\vspace{0.4em}
\textbf{Two decisions owed in week 1:} whether the \texttt{loops} axis enters
the cross-engine claim (three constrained cases, currently zero strong-oracle
coverage); and whether to pin one integrator family and tolerance across all
three engines, since otherwise cross-engine energy drift measures the
integrator rather than the engine.

\vspace{0.6em}
\textbf{Then --- Week 2:} sweep all three, build the comparison table, write.

% ---------------------------------------------------------------------------
\section{Rules in force until the study closes}

\begin{tabular}{@{}cL{0.88\textwidth}@{}}
\textcolor{holdred}{\textbf{!}} & \textbf{The engine does not change.} Fixing MechanicsDSL mid-study is what makes the numbers meaningless. If it moves off \texttt{a8dc2b2}, the baseline is void and the sweep must be repeated. \\
\textcolor{holdred}{\textbf{!}} & \textbf{The scoring path and case matrix are frozen} --- but \textbf{the harness may grow}. Adapters, dispatch, and new oracles are additive. The gate: re-run the frozen matrix at \texttt{a8dc2b2} and confirm it still returns 44/0/1/10 bit-identically. Extending coverage is allowed; revising judgement is not. \\
\textcolor{holdred}{\textbf{!}} & \textbf{Quote numbers only from \texttt{RESULTS.md}.} The 28 July report describes a pre-fix engine and must never be cited as current. \\
\end{tabular}

% ---------------------------------------------------------------------------
\section{Known limit on the headline}

\begin{tabular}{@{}lrrrr@{}}
\toprule
\textbf{Pathway} & \textbf{Cases} & \textbf{Timed out} & \textbf{Adjudicated} & \textbf{Strong oracle} \\
\midrule
lagrangian & 26 & 3 & 23 & 22 \\
hamiltonian & 19 & 5 & 14 & 0 \\
constrained & 10 & 2 & 8 & 0 \\
\midrule
\textbf{Total} & \textbf{55} & \textbf{10} & \textbf{45} & \textbf{22} \\
\bottomrule
\end{tabular}

\vspace{0.3em}
\textbf{33 of 55 cases (60\%) carry no independent derivation check}, and 7 of
those returned no answer at all. The oracle gap and the timeout wall fall in
the \emph{same} place. State the result \textbf{by pathway}, never pooled.

% ---------------------------------------------------------------------------
\section{At a glance}

\begin{tabular}{@{}lL{0.62\textwidth}@{}}
\toprule
Pin              & \texttt{a8dc2b2} \quad tag \texttt{v2.1.3} \quad (\texttt{git checkout v2.1.3}) \\
Headline         & Zero silent failures in 45 adjudicated cases \\
Caveat           & Independent oracle covers 22 of those 45 \\
Timeout          & 180\,s per case \\
Engines          & MechanicsDSL \done \quad SymPy \todo \quad Drake \todo \\
Paper            & Short or workshop paper. \emph{Not} a registered report. \\
\midrule
Plan             & \texttt{stress\_suite/SCOPE.md} \\
Pin record       & \texttt{stress\_suite/FREEZE.md} \\
Numbers          & \texttt{stress\_suite/RESULTS.md} \\
Why they changed & \texttt{stress\_suite/FIXES.md} \\
Repository       & \texttt{Desktop\textbackslash mechanicsdsl-main} \\
\bottomrule
\end{tabular}

\vspace{0.7em}
{\color{rulegrey}\hrule height 0.5pt}
\vspace{0.4em}
{\footnotesize Disclose in the paper: you maintain one of the engines measured,
and AI assistance was used. Both are permitted; concealing either is not.}

\end{document}
